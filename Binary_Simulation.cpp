#include <bits/stdc++.h>
using namespace std;
const int N = 1e5 + 10;
int tree[4 * N],a[N];
// void build(int nd, int st, int en){
    
//     if(st == en){
//         tree[nd] = a[st];

//     }

// }

void update(int )

int main()
{
    int t ;
    cin >> t;
    while (t--)
    {
        string s;
        cin >> s;
        int q; cin >> q;
        while (q--)
        {
            char c;
            cin >> c;
            if(c == 'I'){
                int l, r;
                cin >> l >> r;
                update;
            }
            else{

            }
        }
        
    }
}