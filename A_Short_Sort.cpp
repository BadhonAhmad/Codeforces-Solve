#include <bits/stdc++.h>
using namespace std;

signed main()
{
    int t;
    cin >> t;
    while (t--)
    {
        cin >> s;
        int f = 0;
        if (s[0] = 'a' || s[1] = 'b' || s[2] = 'c')
            f = 1;
        if (f)
            cout << "YES\n";
        else
            cout << "NO\n";
    }
}