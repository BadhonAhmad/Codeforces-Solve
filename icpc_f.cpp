#include <bits/stdc++.h>
#define fastio  ios_base::sync_with_stdio(false); cin.tie(NULL);
#define int long long
#define vll vector<int>
#define all(a) a.begin(), a.end()
#define pb(a) push_back(a)
#define e "\n"
#define yes cout << "YES" << endl
#define no cout << "NO" << endl
const int N=2e5+10;
using namespace std;
/**********************************************************************/
int32_t main()
{
    fastio;
    int cnt = 0,tc, f = 0, sum = 0, ans = 0, t,x, y, z, i, j,cs, k, a, b, c, n, m;
    cin >> tc;
    cs=1;
    while (tc--)
    {
        string s1, s2, s3,s4;
        string b1 = "A+", b2 = "A-", b3 = "Ab+", b4 = "AB-", b5 = "B+", b6 = "B-", b7 = "O+", b8 = "O-";
        vector<int> v;
        map<string, int> bm;
        map<int, int> mp;
        vector<string> sk;
        cnt = 0;
        cin >> s1 >> s2;

        scanf("%lld/%lld/%lld",&x,&y,&z);
        // cin >> s1 >> s2;
        // scanf("%s",s3);
        // cin >> s4;
        //sk.push_back(s4);
        int idx;
        
        //cout << s3.size() << e;
        
       
    }
    return 0;
}

